#!/usr/bin/env ruby
# 1.9 adds realpath to resolve symlinks; 1.8 doesn't
# have this method, so we add it so we get resolved symlinks
# and compatibility
unless File.respond_to? :realpath
  class File #:nodoc:
    def self.realpath path
      return realpath(File.readlink(path)) if symlink?(path)
      path
    end
  end
end
$: << File.expand_path(File.dirname(File.realpath(__FILE__)) + '/../lib')
require 'rubygems'
require 'gli'
require 'pivt'
include GLI

program_desc 'A Pivotal Tracker CLI for getting shit done'
version Pivt::VERSION

config_file File.join(ENV['HOME'],'.pivt.rc.yaml')

desc 'Username'
flag [:u,:username]

desc 'Password'
flag [:p,:password]

desc 'Project ID'
flag :project

desc 'List Pivotal tasks'
long_desc """
Lists Pivotal tasks for the current project.  By default it lists
your current and scheduled tasks only.  A filter can be passed as 
an argument to return different sets of tasks.
"""
command [:list, :l, :ls] do |c|
  c.action do |global_options,options,args|
    @project.print_tasks
  end
end

command [:new, :n] do |c|
  c.action do |global_options,options,args|
    @project.create_task
  end
end

command [:open, :o, :start] do |c|
  c.action do |global_options,options,args|
    @project.start_task(args[0])
  end
end

command [:close, :c, :finish] do |c|
  c.action do |global_options,options,args|
    @project.finish_task(args[0])
  end
end

command [:ship, :s, :deliver] do |c|
  c.action do |global_options,options,args|
    @project.deliver_task(args[0])
  end
end

pre do |global,command,options,args|
  @project = Pivt::Project.new global[:u], global[:p], global[:project]
  true
end

post do |global,command,options,args|
end

on_error do |exception|
  # Error logic here
  # return false to skip default error handling
  true
end

exit GLI.run(ARGV)
